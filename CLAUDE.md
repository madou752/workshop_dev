# Project Rules

## Git commits
- Never run `git commit` as a side effect of writing or editing code.
- Only commit when explicitly asked. When asked, show the proposed commit message and wait for approval before running `git commit`.
- Do not include a `Co-Authored-By: Claude` (or any Claude attribution) trailer in commit messages.
