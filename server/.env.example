# Connection string copied from MongoDB Atlas
# (Atlas > Connect > Drivers). Replace <db_password> with your real password.
MONGODB_URI="mongodb+srv://<user>:<db_password>@<cluster>.mongodb.net/?retryWrites=true&w=majority"

# Database name used inside the cluster
MONGODB_DB="lahistair"
